we can view files in directory by running the following command "ls -lh view"
we can run program by running the following command "./filename"
we can make a directory by running the following command "mkdir"