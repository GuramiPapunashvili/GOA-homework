a = (0,1,2,3,4)
i = n - 1  //*n - 1 უდრის 4ს რადგან aში შენახული ლისთში ბოლო ელემენტი, რომელიც მთავრდება n-1ით, 4ის ტოლია*
MAX = a[i] //*რადგან i უდრის 4ს, MAXი იქნება 4, რადგან a ლისთის მეოთხე ინდექსზე დგას რიცხვი 4*
index = i 
for(i = n - 2; i >= 0; i--){
    if (MAX < a[i]){
        MAX = a[i]
        INDEX = i
    }
}

return INDEX